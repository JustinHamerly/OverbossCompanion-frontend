import { makeStyles } from "@material-ui/core";

export default makeStyles(theme => ({
  activeDisplay: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'top',
    marginLeft: '10px'
  }
}))